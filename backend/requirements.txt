# Versiones verificadas sobre Python 3.13.

# --- API ---
fastapi==0.140.0
uvicorn[standard]==0.51.0
pydantic[email]==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2

# --- Base de datos ---
sqlalchemy==2.0.51
alembic==1.17.2
# Descomentar solo si se usa PostgreSQL en lugar de SQLite:
# psycopg2-binary==2.9.10

# --- Seguridad ---
PyJWT==2.13.0
bcrypt==5.0.0

# --- IA: recomendación y NLP ---
scikit-learn==1.9.0
numpy==2.5.1
scipy==1.18.0
pandas==3.0.5
# Embeddings semánticos para la similitud de contenido del recomendador
# (ver app/ml/recommender.py): capturan significado más allá de la
# coincidencia literal de palabras que daba TF-IDF.
sentence-transformers==5.1.2

# --- Integraciones y tests ---
httpx==0.28.1
pytest==9.1.1

# --- TLS ---
# Usa el almacén de certificados del sistema operativo en vez del bundle de
# certifi: en Windows con inspección TLS corporativa (antivirus, proxy) el
# bundle de certifi no incluye la CA local y httpx falla con
# CERTIFICATE_VERIFY_FAILED aunque el resto del sistema confíe en la conexión.
truststore==0.10.4
